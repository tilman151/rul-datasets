# @package __global__
defaults:
  - override /reader@dm_pre.source.reader: femto
  - override /reader@dm_pre.target.reader: femto

dm_pre:
  source:
    reader:
      fd: 1
      window_size: null
    batch_size: 64
  target:
    reader:
      fd: 2
      window_size: null
      percent_broken: 0.8