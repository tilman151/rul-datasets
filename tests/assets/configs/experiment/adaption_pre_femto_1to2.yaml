# @package __global__
defaults:
  - override /loader@dm_pre.source.loader: femto
  - override /loader@dm_pre.target.loader: femto

dm_pre:
  source:
    loader:
      fd: 1
      window_size: null
    batch_size: 64
  target:
    loader:
      fd: 2
      window_size: null
      percent_broken: 0.8